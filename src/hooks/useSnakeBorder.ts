
const useSnakeBorder = (tagType: string) => {
  const createSnakeBorder = () => {
    for (let i = 0; i < 4; i++) {
      let ele = document.createElement(tagType)
      
    }
  }
  return {
    createSnakeBorder
  }
}

export default useSnakeBorder